---
description: 本地清理与云端历史保留策略（双端共用，index.html 实现）
globs:
  - index.html
  - sw.js
---

# 数据保留与清理

**原则**：除用户主动删除（进垃圾箱 / syncTombstones）外，业务记录不自动删；**完整历史以 Firebase 云端房间为准**，本机可用「从云端拉取最新」恢复。

实现：`DATA_RETENTION`、`runDataRetentionPass()`、`maybeRunScheduledDataRetentionOnStartup()`。

## 主数据存储

- 主键 `todo-app-data-v1` 默认 **IndexedDB**（`exec-system-app-db`），容量远大于 localStorage。
- 首次启动从 localStorage 自动迁移；失败则回退 localStorage。

## 例行清理（每 24h / 保存前轻量）

仅处理：

- **垃圾箱**：超过 30 天的删除项清除；条数上限约 500（非业务数据）
- **重复脏数据**：`pruneRedundantDoneEntriesInState`、`dedupeClosedListWeeklyPlanMirrorsInState`

**不**例行删除：My Done、习惯打卡、Fuel、备忘录、syncTombstones、日记正文。

## 紧急清理（`aggressive`，仅本机存储配额满）

在 IndexedDB/localStorage 仍写不进去时，才可能裁切较旧本机副本（阈值见 `DATA_RETENTION.aggressive*`）。此前应已尝试上传云端；历史查询依赖云端 roomId。

## 修改注意

- 勿在例行 `runDataRetentionPass` 中恢复按年龄删除 My Done / 备忘录等
- 调整阈值只改 `DATA_RETENTION` 与 `TRASH_RETENTION_MS`
- 端别：**Desktop / Mobile / Both** 同一套逻辑
