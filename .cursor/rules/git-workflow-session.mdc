---
description: 任意电脑每次会话必须先提醒 git pull；收尾提醒提交与 push（随仓库同步）
alwaysApply: true
---

# Git 工作流（随仓库同步）

本规则在 `.cursor/rules/` 且已纳入 Git，**任意电脑**执行 `clone` / `git pull` 后内容一致。与端别（Desktop / Mobile）无关，属协作与备份流程。

## 本仓库自动化（Windows / Cursor）

- **打开文件夹**：`.vscode/tasks.json` 中 `runOn: folderOpen` 会尝试 `git pull`；工作区已设 `task.allowAutomaticTasks: on`（若仍提示权限，用命令面板执行「Tasks: Manage Automatic Tasks in Folder」选允许）。
- **结束 Composer 或关闭窗口**：`.cursor/hooks.json` 的 `sessionEnd` 在 `reason` 为 `window_close` / `user_close` 时于系统托盘弹出气泡，提醒检查提交与 `git push`。未打开过 Agent/Composer 就退出时可能不会触发（无会话则无 `sessionEnd`）。

## 开头：先同步远端

在本仓库开始任何实质性改动（含改代码、改配置、跑会改仓库的指令）前，应在本机项目目录先执行：

```bash
git pull
```

若存在未合并冲突或网络问题，先处理完毕再继续。

## 收尾：结束前提交并推送

在结束本轮工作、关闭工程或暂停跨设备协作前，应完成：

1. `git status` 查看是否有未提交变更。
2. 若有需要保留的改动：`git add` → `git commit`（附清晰说明）。
3. `git push` 推送到 GitHub（或当前配置的 `origin`）。

避免长时间只在本地提交不推送，以免换电脑后遗漏版本。

## 对 Agent 的硬性要求（跨设备一致）

- **每次新会话的第一条可见回复**（用户在本仓库发起的新对话、或刚打开项目后的首轮交流）：**必须先**用简短中文提醒：请先在本机项目根目录执行 `git pull` 再开始改仓库；若用户已明确说明「刚 pull 过」「已与远端同步」，可一句带过并不再重复追问。
- **用户任何一轮消息**若表示要开始改代码、继续上次的任务、或请求会修改仓库的操作：若当前对话尚未提醒过拉取，则**必须先提醒** `git pull`（同上例外）。
- **会话收尾或用户表示要结束、关机、换机器时**：提醒检查未提交变更，并执行提交与 `git push`（由用户在本机终端操作）。
- 不代替用户执行写入远端或凭证相关操作；仅作提醒与命令说明。

## 例外

- 用户明确表示本次仅本地实验、暂不上传。
- 仓库仅为只读查阅、无推送权限时，收尾改为提醒提交或补丁备份即可。
- 用户仅问概念题、与仓库无关且不涉及改项目时，可不强行插入 `git pull`；一旦涉及本仓库文件或 Git，仍应先确认同步。
