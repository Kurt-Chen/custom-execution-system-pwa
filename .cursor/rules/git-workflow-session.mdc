---
description: 会话以 git pull 开头、以提交与 git push 收尾；结束前提醒同步 GitHub
alwaysApply: true
---

# Git 工作流（随仓库同步）

本规则写入仓库，任意电脑 `clone` / `pull` 后同样生效。与端别（Desktop / Mobile）无关，属协作与备份流程。

## 开头：先同步远端

在本仓库开始一轮实质性改动前，应先执行：

```bash
git pull
```

若存在未合并冲突或网络问题，先处理完毕再继续改代码。

## 收尾：结束前提交并推送

在结束本轮工作、关闭工程或暂停跨设备协作前，应完成：

1. `git status` 查看是否有未提交变更。
2. 若有需要保留的改动：`git add` → `git commit`（附清晰说明）。
3. `git push` 推送到 GitHub（或当前配置的 `origin`）。

避免长时间只在本地提交不推送，以免换电脑后遗漏版本。

## 对 Agent 的要求

- **会话开始时**：若用户表示刚开始在本仓库做事，可主动提示「是否已 `git pull`」，尚未拉取则提醒先拉取。
- **会话收尾或用户表示要结束、关机、换机器时**：提醒检查未提交变更，并执行提交与 `git push`（用户在本机终端操作）。
- 不代替用户执行写入远端或凭证相关操作；提醒与命令说明即可。

## 例外

- 用户明确表示本次仅本地实验、暂不上传。
- 仓库仅为只读查阅、无推送权限时，收尾改为提醒提交或补丁备份即可。
