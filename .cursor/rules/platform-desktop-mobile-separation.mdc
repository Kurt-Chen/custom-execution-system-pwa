---
description: 区分电脑版与手机版改动范围
alwaysApply: true
---

# 电脑版与手机版修改规则

每次开始改动前，必须先明确本次需求属于以下哪一类：
- 仅电脑版（Desktop）
- 仅手机版/平板（Mobile/Tablet）
- 双端都要改

## 执行要求

- 未明确端别前，不直接改代码；先确认范围。
- 做移动端适配时，优先使用明确媒体查询（如 `max-width`、`pointer: coarse`）隔离影响。
- 做桌面端修改时，避免影响移动端规则；必要时加桌面端覆盖。
- 若同一组件双端样式不同，必须分别声明并分别修改，不混写成一套通用规则。
- 修改后说明中必须包含“本次影响端别”（Desktop / Mobile / Both）。

## 默认安全策略

- 如果用户只说“继续/优化”，默认先按最近一次已确认端别执行。
- 若存在歧义，先询问再修改，避免误伤另一端样式。
